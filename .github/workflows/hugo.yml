name: Deploy Hugo site to GitHub Pages

on:
  push:
    branches:
      - main
  repository_dispatch:
    types: [resume-published]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 0

      - name: Setup mise
        uses: jdx/mise-action@v3

      - name: Fetch resume markdown for self-hosting
        env:
          GH_TOKEN: ${{ github.token }}
        run: scripts/fetch-resume.sh

      - name: Validate PDF assets
        run: mise run validate:pdf

      - name: Verify self-hosted resume markdown published
        run: |
          set -euo pipefail
          test -s public/resume/resume.txt
          test -s public/resume/resume.structured.txt

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: ./public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
